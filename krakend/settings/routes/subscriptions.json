{
  "routes": [
    {
      "endpoint": "/subscriptions",
      "method": "GET",
      "": "{{ include \"common/input-headers/auth.tmpl\"  }}",
      "extra_config": {
        "": "{{ include \"common/extra-config/proxy.tmpl\"  }}"
      },
      "backend": [
        {{ include "common/services/backend.tmpl" }},
        {
          "url_pattern": "subscriptions",
          "method": "POST",
          "host": ["http://microservice02:3001"],
          "encoding": "json",
          "extra_config": {
            {{ include "common/extra-config/modifier-authorization-token.tmpl" }}
          }
        }
      ]
    }
  ]
}